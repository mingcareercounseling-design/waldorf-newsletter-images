---
// 清除 admin auth cookie，重導向回登入頁
if (Astro.request.method === 'GET') {
  return new Response(null, {
    status: 302,
    headers: {
      'Location': '/admin',
      'Set-Cookie': 'admin_auth=; Path=/admin; HttpOnly; SameSite=Strict; Max-Age=0'
    }
  });
}
---
